let result = '';

const name1 = prompt('Enter 1 name');

result = `${result} ${name1}`;

const name2 = 'Bohdan';

result = `${result} ${name2}`;

const name3 = prompt('Enter another name');

result = `${result} ${name3}`;

alert(result);
